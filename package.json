{
  "name": "vaultpass-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "contracts",
    "app"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=app",
    "build": "npm run build --workspace=app",
    "test": "npm run test --workspace=contracts",
    "lint": "npm run lint --workspace=app"
  }
}
